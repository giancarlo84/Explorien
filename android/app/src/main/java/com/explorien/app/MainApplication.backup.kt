package com.explorien.app

import android.app.Application

class `MainApplication.backup` : Application() {
  override fun onCreate() {
    super.onCreate()
    // This is a temporary placeholder
  }
}
